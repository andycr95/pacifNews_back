# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: "Realizando test a develop"

on:
  push:
    branches: [ "Develop" ]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_DATABASE: pacificNews_test
          MYSQL_USER: pacificode
          MYSQL_PASSWORD: pacificode
          MYSQL_ROOT_PASSWORD: pacificode
        ports:
          - 33306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    env:
      DB_DATABASE: pacificNews_test
      DB_USER: root
      DB_PASSWORD: pacificode

    strategy:
      matrix:
        node-version: [16.x]
    
    steps:

    - uses: actions/checkout@v3

    - name: Make envfile
      uses: SpicyPizza/create-envfile@v1.3
      with:
        envkey_DATABASE_URL: mysql://root:pacificode@127.0.0.1:3306/pacificNews_test?schema=public
        envkey_ACCESS_TOKEN_SECRET: unipacifico
        file_name: .env
        fail_on_empty: false

    - name: Set up DATABASE
      run: |
        sudo /etc/init.d/mysql start
        mysql -e 'CREATE DATABASE ${{ env.DB_DATABASE }};' -u${{ env.DB_USER }} -p${{ env.DB_PASSWORD }}

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - run: npm ci

    - run: npm test
